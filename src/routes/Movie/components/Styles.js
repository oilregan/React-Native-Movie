import React from 'react';
import {
    StyleSheet
} from 'react-native';

export const Styles = StyleSheet.create({
    container: {
        height: 250,
        flexDirection: 'row',
        justifyContent: "flex-start",
        resizeMode: "cover",
    }
});